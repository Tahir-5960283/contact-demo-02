export interface Contact {
  id: number;
  firstName: string;
  lastName: string;
  role: string;
  email: string;
  alternateEmail?: string;
  dial?: string;
  meeting?: string;
  phone?: string;
  image?: string;
  bio?: string;
}
